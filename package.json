{
  "name": "violet",
  "version": "0.0.0",
  "private": true,
  "description": "Violet — watch brand. Monorepo: international reference site (EN/RU/AR) + Persian B2B order app (FA), shared design system. See docs/.",
  "packageManager": "pnpm@10.16.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.6.3"
  }
}
