Indeterminate loading ring. Prefer `Skeleton` for content placeholders; use `Spinner` for actions in progress.

```jsx
<Spinner />
<Spinner size={32} thickness={3} />
<Spinner style={{ color: '#fff' }} />   {/* on a dark button */}
```
