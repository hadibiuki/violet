Shimmering placeholder that holds the layout while data loads — the brand standard for catalog/PDP loading (no spinners, no layout shift).

```jsx
<Skeleton variant="card" />                 {/* product card */}
<Skeleton variant="text" lines={3} />
<Skeleton variant="circle" width={36} />
<Skeleton height={40} />                     {/* a bar */}
```
