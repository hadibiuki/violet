/**
 * @violet/ui — shared component library.
 *
 * PLACEHOLDER (scaffold only): components (Button, Input, Badge, Order Status,
 * Product Card, …) are added on top of this skeleton next. They bind to the
 * `@violet/tokens` CSS variables, use headless primitives (Radix) for keyboard
 * /focus a11y, and are RTL-safe via logical properties (docs/a11y-checklist.md,
 * docs/i18n-rtl.md). Re-export each component from here as it lands.
 */

export {};
